String AP_NAME = "esp_"; // ap = wifi access point (microcontroller is now a hot spot)
String AP_PASSWORD = "stringstring";

// connect microcontroller to the hot spot (5GHz => 2.4 GHz)

char* CLI_SSID = "smn";    // wifi client (microcontroller is now a client), ssid = a network name you want to connect to
char* CLI_PASS = "aeaw6550";

char* mqtt_broker = "broker.emqx.io";
const int mqtt_port = 1883;

int WEB_SERVER_PORT = 80;


